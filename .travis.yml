language: c
sudo: false
compiler:
  - clang

branches:
  only:
    - master

script: mkdir -p build && cd build && cmake .. && make && make test && mkdir -p buildnox64 && cd buildnox64 && cmake -DDISABLE_X64=ON .. && make && make test
